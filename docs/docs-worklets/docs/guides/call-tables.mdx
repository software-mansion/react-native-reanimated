---
id: call-tables
sidebar_label: Call tables
---

# Call tables

Call tables are quick references that show **where a given scheduling function can be called from**, depending on whether [Bundle Mode](/docs/bundle-mode) is enabled or not.

### Why does Bundle Mode matter?

Without Bundle Mode, Worklet Runtimes only have a subset of functionalities of the RN Runtime, due to limitations of serialization of worklets. With Bundle Mode enabled, every runtime has access to the full JavaScript bundle, so most threading APIs become available on all runtimes.

### How to read a call table

Take [scheduleOnUI](/docs/threading/scheduleOnUI) as an example:

import CallTable from '@site/src/components/CallTable';

<CallTable
  bundleMode={{
    rnRuntime: true,
    uiRuntime: true,
    workerRuntime: true,
  }}
  noBundleMode={{
    rnRuntime: true,
    uiRuntime: false,
    workerRuntime: false,
  }}
  fnName="scheduleOnUI"
/>

`Yes` in the table means that the function can be called from that runtime, while `No` means that calling the function from that runtime will throw an error. So in this case, `scheduleOnUI` can be called from any runtime when Bundle Mode is on, but only from the RN Runtime when Bundle Mode is off:

```tsx {5-7,10-12,16-18}
import { scheduleOnUI, createWorkletRuntime } from 'react-native-worklets';

const workerRuntime = createWorkletRuntime();

// We are on RN Runtime - 
// this is always fine
scheduleOnUI(...);

scheduleOnUI(() => {
    // We are on the UI Runtime - 
    // this is only fine with Bundle Mode enabled
  scheduleOnUI(...)
});

scheduleOnRuntime(workerRuntime, () => {
    // We are on the Worker Runtime - 
    // this is only fine with Bundle Mode enabled
    scheduleOnUI(...)
});
```
