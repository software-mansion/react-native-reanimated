{
  "name": "ReanimatedPlugin",
  "dependencies": {
    "convert-source-map": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "license": "MIT",
  "readmeFilename": "README.md",
  "scripts": {
    "postinstall": "yarn tsc && yarn format",
    "format": "prettier --write --list-different '.'",
    "clear": "rm -f *.js && rm -f *.js.map && rm -rf dec"
  }
}
