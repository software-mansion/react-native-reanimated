---
sidebar_position: 5
---

# DynamicColorIOSAnimated

`DynamicColorIOSAnimated` is a workletized version of a [`DynamicColorIOS`](https://reactnative.dev/docs/dynamiccolorios) to use within Reanimated. It's a way to define colors that automatically adap to light and dark mode on iOS.

## Reference

```tsx
import { DynamicColorIOSAnimated } from 'react-native-reanimated';

function Example() {
  const progress = useSharedValue(0);

  const animatedStyle = useAnimatedStyle(() => {
    // ...

    return {
      backgroundColor: DynamicColorIOSAnimated({
        light: lightColor,
        dark: darkColor,
      }),
    };
  });
}
```

<details>
<summary>Type definitions</summary>

```typescript
function DynamicColorIOSAnimated(
  {
    light: ColorValue,
    dark: ColorValue,
    highContrastLight?: ColorValue,
    highContrastDark?: ColorValue
  }
): DynamicColorIOSReturnType;

type DynamicColorIOSReturnType = {
  dynamic?: {
    light: ColorValue,
    dark: ColorValue,
    highContrastLight?: ColorValue,
    highContrastDark?: ColorValue
  },
}
```

</details>

### Arguments

#### `tuple`

Object with two mandatory keys: `light` and `dark`, and two optional keys: `highContrastLight` and `highContrastDark`.

### Returns

Object containing `dynamic` field, where the input is injected.

### Example

import DynamicColorIOSExample from '@site/src/examples/DynamicColorIOSExample';
import DynamicColorIOSExampleSrc from '!!raw-loader!@site/src/examples/DynamicColorIOSExample';

<CollapsibleCode src={DynamicColorIOSExampleSrc} showLines={[20, 39]} />

## Platform compatibility

<PlatformCompatibility ios />
