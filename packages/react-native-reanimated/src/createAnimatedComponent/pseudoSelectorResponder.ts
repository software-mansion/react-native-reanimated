'use strict';

export function addPseudoSelectorResponder(_props: Record<string, unknown>) {
  // Web resolves pseudo selectors with plain CSS, so the element doesn't have
  // to become a touch target.
}
