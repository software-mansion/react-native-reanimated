Fix two shipped type declarations that break consumers under `exactOptionalPropertyTypes` or `skipLibCheck: false` - `DecayAnimation.current` no longer widens its ancestor's optional property to an explicit `| undefined`, and the global `Navigator` augmentation declares `userAgent` and `vendor` `readonly` to match the DOM lib.
