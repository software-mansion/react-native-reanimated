import {
  cond,
  sub,
  divide,
  multiply,
  add,
  block,
  set,
  greaterOrEq,
  defined,
} from '../base';

export default function sequence(clock, state, config) {
  return block([
    state.position, //TODO
  ]);
}
