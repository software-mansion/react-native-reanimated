name: npm worklets publish check
permissions:
  contents: read
  id-token: write
on:
  pull_request:
    branches:
      - 'worklets-[0-9]+.[0-9]+-stable'

jobs:
  npm-worklets-publish-check:
    uses: ./.github/workflows/npm-worklets-publish.yml
